import gameDataObject from './GAME_YEAR';

/**
 * Gets basic information about the FRC game loaded into the system.
 * @returns An object with metadata about the currently selected game
 */
const gameData = gameDataObject;

export default gameData;